# shellcheck shell=bash
use flake

export NH_FLAKE=~/nix-config

# Local secrets and machine-specific overrides live in .envrc.local, which is gitignored.
if [ -f .envrc.local ]; then
  # shellcheck disable=SC1091
  source .envrc.local
  watch_file .envrc.local
fi
